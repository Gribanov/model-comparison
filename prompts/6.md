You are working on a Symfony project for a Telegram bot.

Goal: implement automatic cleanup of stale temporary subtitle files and working directories.

Context:
- The project already has Docker, Symfony, Redis, Messenger, Telegram webhook support, URL validation, per-user Redis locking, and an async worker that downloads subtitles with yt-dlp and sends them to Telegram.
- Subtitle files are stored temporarily in a configured temp directory.
- Files should already be removed after successful upload on the happy path.
- This task is about cleaning up leftovers that remain after crashes or unexpected failures.
- Cleanup must run once per hour.
- All PHP/Symfony commands must be run inside Docker containers.
- Follow AGENTS.md if present.
- Use the real project structure and service names from the repository instead of guessing.

Business rules:
- Stale temp files and directories must be removed automatically.
- The cleanup policy should be time-based.
- The configured temp directory must remain under application control.
- Cleanup should be safe and predictable.

Requirements:

1. Implement a dedicated temp file cleanup service.
    - It should work against the configured subtitle temp root directory.
    - It should remove stale files and per-job directories older than a configured TTL.
    - TTL should come from config/env, e.g. TEMP_FILE_TTL.
    - Keep behavior explicit and deterministic.
    - Avoid deleting anything outside the configured temp root.

2. Implement a Symfony console command for cleanup.
    - Suggested name: app:subtitle-temp:cleanup
    - The command should:
        - scan the temp directory
        - delete stale files/directories older than TTL
        - report how many items were deleted
        - handle missing temp directory gracefully
    - The command should be safe to run repeatedly.

3. Add Docker-based hourly scheduling for cleanup.
    - Implement an hourly scheduler mechanism appropriate for this Docker project.
    - Prefer a clean Docker-oriented solution rather than assuming host cron.
    - It is acceptable to add a dedicated lightweight scheduler container if that is the best fit.
    - Document how cleanup is scheduled and where it runs.
    - Keep the setup simple and production-friendly.

4. Keep architecture clean.
    - Cleanup policy logic should be in a dedicated service.
    - Command should delegate to the service.
    - Scheduling should be configured in Docker/infrastructure, not hardcoded in PHP logic.

5. Add configuration wiring.
    - Ensure temp directory path is configurable
    - Ensure cleanup TTL is configurable
    - Provide sensible example env values

6. Add automated tests where practical.
    - Deletion of stale files
    - Preservation of fresh files
    - Safe handling of missing directory
    - Command behavior if practical in the current test setup

7. Update README.
    - Explain temp file lifecycle
    - Explain that files are deleted immediately after successful upload
    - Explain that stale leftovers are cleaned hourly
    - Document how to run the cleanup command manually inside Docker

Important constraints:
- Do not add database
- Do not add unrelated refactors
- Do not weaken the happy-path cleanup that already exists
- Keep cleanup scoped strictly to the configured temp directory

After implementing:
- Run relevant tests inside Docker
- Validate the cleanup command inside Docker if practical
- Verify the hourly scheduling setup is coherent

At the end, generate a commit message in English describing the changes.
