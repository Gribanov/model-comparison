You are working on a Symfony project for a Telegram bot.

Goal: implement per-user active job protection and dispatch subtitle download jobs to Symfony Messenger.

Context:
- The project already has Docker, Symfony, Redis, Messenger, Telegram webhook support, and YouTube URL validation.
- The bot must allow only one active job per Telegram user at any moment.
- Redis is already available and should be used for both Messenger transport and active-job locking.
- Long-running work must not happen in the webhook controller.
- All PHP/Symfony commands must be run inside Docker containers.
- Follow AGENTS.md if present.
- Use the real repository structure and service names instead of guessing.

Business rules:
- If a user sends a valid YouTube video URL and has no active job, the bot should queue the job and confirm it to the user.
- If the same user already has an active job, the bot should not queue a new one and should send a friendly status message instead.
- Only one active job per Telegram user is allowed.
- There is no persistent database/job history in this version.

Requirements:

1. Implement a Redis-backed lock service for active jobs per Telegram user.
    - Encapsulate lock operations in a dedicated service.
    - Prefer atomic Redis semantics similar to SET key value NX EX.
    - Use a configurable TTL from env/config, e.g. USER_JOB_LOCK_TTL.
    - Provide methods similar to:
        - acquire(userId): bool
        - release(userId): void
        - isActive(userId): bool
    - Keep key naming explicit and predictable.

2. Introduce a Messenger message for background processing.
    - Suggested name: DownloadSubtitlesMessage
    - Include at least:
        - telegram user id
        - chat id
        - youtube url

3. Update incoming message orchestration.
    - After validating a YouTube URL:
        - try to acquire per-user lock
        - if lock acquisition fails:
            - send a user-friendly Telegram message that their previous request is still being processed
        - if lock acquisition succeeds:
            - dispatch DownloadSubtitlesMessage to the async Messenger transport
            - send a user-friendly confirmation that subtitles were queued

4. Keep responsibilities separated.
    - Controller stays thin
    - Lock logic stays in dedicated service
    - Queue dispatch logic stays in application/orchestration service
    - Telegram reply logic stays in Telegram client/service
    - Do not implement yt-dlp execution in this step

5. Add configuration wiring.
    - Add env/config for lock TTL
    - Ensure Messenger routing sends DownloadSubtitlesMessage to async transport

6. Add automated tests.
    - Lock service behavior
    - Dispatch behavior for first valid request
    - Rejection behavior for second request while lock exists
    - Ensure invalid URLs still do not acquire locks or dispatch jobs
    - Test the orchestration logic as directly as practical

7. Update README if useful.
    - Document the one-active-job-per-user rule
    - Mention that jobs are queued asynchronously via Messenger/Redis

Important constraints:
- Do not add database
- Do not implement subtitle downloading yet
- Do not implement cleanup command yet
- Keep everything ready for the next step where the worker will actually run yt-dlp

After implementing:
- Run relevant tests inside Docker
- Validate Messenger routing/config inside Docker if practical

At the end, generate a  commit message in English describing the changes.
