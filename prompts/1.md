You are working on a new Symfony-based project.

Goal: bootstrap a minimal working project structure for a Telegram bot that will later download YouTube subtitles.

Requirements:

1. Create a Symfony project (latest stable version).
2. Add basic dependencies:
    - symfony/framework-bundle
    - symfony/console
    - symfony/messenger
    - symfony/http-client
    - symfony/runtime
3. Configure Symfony Messenger to use Redis as transport.
    - Use environment variable MESSENGER_TRANSPORT_DSN
    - Define one transport named "async"
4. Prepare project for Docker usage:
    - Create docker-compose.yml with services:
        - app (PHP)
        - caddy
        - redis
        - worker (same image as app, running messenger consumer)
    - PHP container must include:
        - PHP CLI
        - required extensions for Symfony
        - ability to run Composer
    - Install yt-dlp inside the PHP image (binary install is preferred)
5. Configure working directories:
    - Symfony app inside /app
    - temp files directory configurable via env (APP_SUBTITLE_TEMP_DIR)
6. Add example .env variables:
    - TELEGRAM_BOT_TOKEN
    - TELEGRAM_WEBHOOK_BASE_URL
    - TELEGRAM_WEBHOOK_SECRET
    - MESSENGER_TRANSPORT_DSN
    - REDIS_PASSWORD
    - YTDLP_BIN
    - APP_SUBTITLE_TEMP_DIR
7. Ensure all commands are intended to run inside Docker containers.
8. Add a README section explaining how to:
    - build containers
    - start services
    - run Symfony commands inside container
    - start worker

Important constraints:
- Do NOT implement business logic yet.
- Do NOT add database.
- Everything must be Docker-first.

After implementing:
- Verify messenger config is valid.
- Verify containers can start.

At the end, generate a commit message in English describing the changes.
