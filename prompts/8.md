You are working on a Symfony project for a Telegram bot.

Goal: add a minimal end-to-end verification workflow and deployment smoke-test support for the MVP subtitle bot.

Context:
- The project already includes:
    - Docker / Docker Compose
    - Symfony application
    - Telegram webhook endpoint
    - webhook sync command
    - YouTube URL validation
    - Redis-backed Messenger queue
    - per-user active job locking in Redis
    - async worker using yt-dlp to fetch subtitles
    - Telegram document upload
    - hourly cleanup of stale temp files
    - basic hardening and documentation
- The bot accepts only regular YouTube video links and returns subtitles as an .srt file.
- All PHP/Symfony commands must be run inside Docker containers.
- Follow AGENTS.md if present.
- Use the real repository structure and service names instead of guessing.

Goal of this step:
Make it easy to verify that the whole MVP works after deployment or before release, without adding major new features.

Requirements:

1. Add a practical smoke-test approach for the deployed system.
    - Provide a small, safe verification path that confirms the app is basically operational.
    - It may include one or more console commands, scripts, or documented manual checks.
    - Focus on verifying:
        - Symfony app container is healthy enough
        - Redis is reachable
        - temp directory is writable
        - webhook configuration can be checked
        - worker command is runnable
        - yt-dlp binary is available in the container
    - Keep it lightweight and production-friendly.

2. Add a diagnostic console command if useful.
    - Suggested examples:
        - app:bot:diagnostics
        - app:system:check
    - It may check:
        - required env/config presence
        - temp directory accessibility
        - yt-dlp binary existence/executability
        - Redis connectivity if practical
    - Output should be human-readable and useful for operators.
    - Fail clearly when required components are unavailable.

3. Add a documented manual end-to-end test scenario.
    - Document how to:
        - start the stack
        - sync webhook
        - send a real Telegram message containing a supported YouTube URL
        - confirm the message enters the queue
        - confirm the worker processes it
        - confirm the bot sends an .srt file back
    - Include expected outcomes and likely failure points.
    - Use Docker-based commands only.

4. Improve operational logging/documentation if helpful.
    - Document where to inspect logs for:
        - webhook handling
        - worker processing
        - yt-dlp failures
        - cleanup command
    - Do not add heavy observability systems.
    - Keep it minimal and practical.

5. Add tests only where they meaningfully support the new diagnostic/smoke-test behavior.
    - Prefer focused tests for any new diagnostic service/command.
    - Avoid broad noisy test additions.
6. Add information to a readme file on how to deploy this project on VPS using ssh access
7. Keep scope controlled.
    - Do not add database
    - Do not add admin UI
    - Do not add CI/CD unless the repository already has a clear place for it and a tiny addition is obviously useful
    - Do not redesign infrastructure
    - Do not add major new features

Deliverables:
- a lightweight diagnostic/smoke-test implementation if appropriate
- updated README with an operator-friendly verification checklist
- any focused tests you consider necessary
- a short summary of how to verify the MVP after deployment

After implementing:
- Run relevant tests inside Docker
- Run the new diagnostic/smoke-test command inside Docker if implemented
- Verify the documentation matches the actual project commands and service names

At the end, generate a commit message in English describing the changes.
