You are working on a Symfony project for a Telegram bot.

Goal: perform final hardening and operational polish for the MVP subtitle bot.

Context:
- The project already includes:
    - Docker / Docker Compose
    - Symfony application
    - Telegram webhook endpoint
    - webhook sync command
    - YouTube URL validation
    - Redis-backed Messenger queue
    - per-user active job locking in Redis
    - async worker using yt-dlp to fetch subtitles
    - Telegram document upload
    - hourly cleanup of stale temp files
- The bot accepts only regular YouTube video links and returns subtitles as an .srt file.
- All PHP/Symfony commands must be run inside Docker containers.
- Follow AGENTS.md if present.
- Use the real repository structure and service names instead of guessing.

Goal of this step:
Make the MVP production-ready enough for a first deploy by improving configuration, robustness, documentation, and developer experience without changing the main business flow.

Requirements:

1. Review and harden configuration.
    - Ensure all important values are environment-driven.
    - Check for missing configuration validation or unclear defaults.
    - Review at least:
        - TELEGRAM_BOT_TOKEN
        - TELEGRAM_WEBHOOK_BASE_URL
        - TELEGRAM_WEBHOOK_SECRET
        - MESSENGER_TRANSPORT_DSN
        - YTDLP_BIN
        - APP_SUBTITLE_TEMP_DIR
        - USER_JOB_LOCK_TTL
        - TEMP_FILE_TTL
    - Improve parameter naming and config wiring if needed.
    - Avoid hardcoded secrets and hardcoded infrastructure values.

2. Improve robustness and error handling.
    - Review failure paths in webhook handling, queue dispatch, yt-dlp execution, Telegram upload, lock handling, and cleanup.
    - Tighten logging where useful.
    - Ensure end-user messages stay simple and safe.
    - Make sure lock release and temp cleanup behavior are reliable.
    - Remove obvious fragile spots or duplicated logic if found.

3. Add or improve health/developer utilities where useful.
    - It is acceptable to add a small diagnostic console command if it helps operations, for example checking bot-related configuration or validating temp directory accessibility.
    - Keep it small and practical.
    - Do not add unnecessary features.

4. Improve README and project documentation.
    - Document the full MVP flow:
        - webhook reception
        - queue dispatch
        - worker processing
        - yt-dlp subtitle fallback logic
        - Telegram reply behavior
        - one active job per user rule
        - temp cleanup behavior
    - Document local/dev startup commands using Docker.
    - Document deployment-oriented commands:
        - webhook sync
        - worker start
        - manual cleanup
    - Document supported and unsupported YouTube URL formats.
    - Document important environment variables.

5. Review Docker/dev ergonomics.
    - Ensure service names and command examples in README match actual files.
    - Ensure worker/scheduler/app services are clearly documented.
    - Improve Docker-related comments or config readability if helpful.
    - Do not redesign the whole infrastructure unless necessary.

6. Add or improve tests where practical.
    - Cover any fragile logic that is currently untested.
    - Prioritize meaningful tests over broad noisy coverage.
    - Do not add low-value tests just for the sake of numbers.

7. Keep scope controlled.
    - Do not add database
    - Do not add admin UI
    - Do not add support for non-YouTube platforms
    - Do not add playlists or Shorts support
    - Do not implement retries unless there is already a broken partial implementation that needs fixing
    - Do not introduce broad refactors unrelated to MVP stability

Deliverables:
- code/config/doc improvements for MVP hardening
- updated README
- any focused tests you consider necessary
- a short summary of what was improved and any remaining caveats

After implementing:
- Run relevant tests inside Docker
- Validate main Symfony commands inside Docker if practical
- Verify configuration and documentation consistency

At the end, generate a commit message in English describing the changes.
