You are working on a Symfony project for a Telegram bot.

Goal: implement Telegram webhook receiving and webhook synchronization.

Context:
- The project already has Docker, Symfony, Redis, Messenger, and yt-dlp installed.
- The bot will receive Telegram updates via webhook.
- Configuration comes from environment variables.
- All PHP/Symfony commands must be run inside Docker containers.
- Follow the guidance from AGENTS.md if present.
- Use the real project structure and service names from the repository instead of guessing.

Requirements:

1. Implement a Telegram webhook HTTP endpoint.
    - Create a controller for receiving Telegram webhook updates.
    - The route must include a secret path segment based on TELEGRAM_WEBHOOK_SECRET.
    - Accept POST requests from Telegram.
    - Return a valid HTTP response quickly.
    - Do not perform long-running work in the controller.

2. Parse incoming Telegram updates.
    - For now, support only text messages.
    - Extract:
        - chat id
        - user id
        - message text
    - Ignore unsupported update types safely.

3. Create an application service that handles the incoming update.
    - Keep controller thin.
    - Do not start yt-dlp here.
    - Do not dispatch jobs yet unless needed for wiring demonstration.
    - It is fine to leave a placeholder for later queue integration.

4. Implement Telegram API client/service for webhook operations.
    - Add methods for:
        - getWebhookInfo
        - setWebhook
    - Use Symfony HttpClient or another Symfony-native solution.
    - Read bot token from configuration.

5. Implement a console command to synchronize webhook registration.
    - Suggested command name: app:telegram:webhook:sync
    - Build expected webhook URL from:
        - TELEGRAM_WEBHOOK_BASE_URL
        - TELEGRAM_WEBHOOK_SECRET
    - Call getWebhookInfo
    - If current webhook already matches expected URL, report success and do nothing
    - Otherwise call setWebhook and report result
    - Handle Telegram API failures gracefully

6. Add configuration wiring.
    - Bind env vars properly
    - Keep secrets out of code
    - Keep URL generation explicit and predictable

7. Add tests where practical.
    - At least cover webhook URL generation and webhook sync decision logic
    - If functional controller tests are easy in current setup, add them too

8. Update README with:
    - how to run webhook sync command inside Docker
    - expected webhook URL shape
    - any assumptions needed for local testing

Important constraints:
- Do not add database
- Do not implement subtitle downloading yet
- Do not implement full queue flow yet
- Keep responsibilities separated
- Prefer small focused services and constructor injection

After implementing:
- Run relevant tests inside Docker
- Validate the command and configuration as much as possible

At the end, generate a commit message in English describing the changes.
