You are working on a Symfony project for a Telegram bot.

Goal: implement the async worker flow that downloads YouTube subtitles with yt-dlp and sends them back to the user as an .srt file.

Context:
- The project already has Docker, Symfony, Redis, Messenger, Telegram webhook support, YouTube URL validation, and Redis-based per-user active job locking.
- A DownloadSubtitlesMessage is already dispatched to the async transport.
- yt-dlp is installed inside the application/worker Docker image.
- The bot must return subtitles as an .srt file.
- The bot should first try regular subtitles.
- If regular subtitles are unavailable, it should try auto-generated subtitles.
- If neither exists, it should send a user-friendly failure message.
- After successful Telegram upload, the subtitle file must be deleted.
- The user lock must be released when processing finishes, whether successful or failed.
- All PHP/Symfony commands must be run inside Docker containers.
- Follow AGENTS.md if present.
- Use the real project structure and service names from the repository instead of guessing.

Business rules:
- Only YouTube links are supported.
- Only one active job per Telegram user is allowed.
- No database/job history is needed in this version.
- No automatic retry logic is needed in this version.

Requirements:

1. Implement a dedicated yt-dlp subtitle downloader service.
    - Encapsulate all yt-dlp command construction and execution in one dedicated service.
    - Use Symfony Process or another Symfony-native approach.
    - Read yt-dlp binary path from config/env (e.g. YTDLP_BIN).
    - Use a configured temp directory (e.g. APP_SUBTITLE_TEMP_DIR).
    - Create an isolated per-job working directory.
    - Do not download the video itself.
    - First attempt regular subtitles.
    - If regular subtitles are unavailable, attempt auto-generated subtitles.
    - Request .srt output.
    - Return a structured result or throw a dedicated exception when subtitles are unavailable.

2. Implement the Messenger handler for DownloadSubtitlesMessage.
    - Create or complete DownloadSubtitlesHandler.
    - In the handler:
        - invoke the subtitle downloader
        - locate the resulting .srt file
        - send the file back to the Telegram user as a document
        - delete the file and job temp directory after successful upload
    - Always release the per-user active-job lock at the end, including error cases.

3. Extend Telegram API client/service.
    - Implement sendDocument support for file upload to Telegram.
    - Keep file upload logic encapsulated in the Telegram client/service.
    - Preserve sendMessage support for error/status messages.

4. Handle failure cases clearly.
    - If no subtitles are available:
        - send a user-friendly failure message to the user
    - If yt-dlp execution fails unexpectedly:
        - log useful diagnostic information
        - send a generic user-friendly error message
    - If Telegram upload fails:
        - log the failure
        - still release the lock
    - Avoid leaking sensitive internals to end users

5. Keep responsibilities separated.
    - Controller remains thin
    - Handler performs async orchestration only
    - yt-dlp execution stays in a dedicated service
    - file/temp directory operations stay encapsulated if a temp file manager already exists or if introducing one improves clarity
    - lock release should happen in a reliable finally-style path

6. Add configuration wiring where needed.
    - ytdlp binary path
    - temp directory
    - optional process timeout if useful

7. Add automated tests where practical.
    - Unit tests for command-building/downloader orchestration where possible
    - Handler behavior with mocked downloader/client/lock services
    - Success path
    - No-subtitles path
    - Unexpected failure path
    - Ensure lock release is covered in tests

8. Update README if useful.
    - Explain the worker responsibility
    - Explain the fallback from regular subtitles to auto-generated subtitles
    - Mention where temp files are stored and that they are deleted after upload

Important constraints:
- Do not add database
- Do not add automatic retry logic
- Do not implement hourly cleanup yet in this step
- Keep the implementation production-oriented but minimal

After implementing:
- Run relevant tests inside Docker
- Validate the worker command and async handling inside Docker if practical
- Ensure no temp files are left behind on the happy path

At the end, generate a commit message in English describing the changes.
