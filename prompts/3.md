You are working on a Symfony project for a Telegram bot.

Goal: implement validation and orchestration for incoming Telegram text messages that contain YouTube video URLs.

Context:
- The project already has Docker, Symfony, Redis, Messenger, and Telegram webhook support.
- The bot should support only YouTube video links.
- Allowed domains:
    - youtube.com
    - www.youtube.com
    - youtu.be
- Supported links:
    - regular YouTube video links only
- Unsupported:
    - playlists
    - channels
    - Shorts
    - non-YouTube URLs
    - malformed URLs
- All PHP/Symfony commands must be run inside Docker containers.
- Follow AGENTS.md if present.
- Use the real project structure and service names from the repository instead of guessing.

Requirements:

1. Implement a dedicated YouTube URL validator/parser service.
    - It should accept raw message text or an extracted URL string.
    - It should validate that the URL is a supported YouTube video URL.
    - It should support both:
        - youtube.com/watch?v=...
        - youtu.be/<video-id>
    - It should reject Shorts, playlists, channels, and non-video URLs.
    - It should normalize the accepted URL into a consistent canonical video URL format if practical.

2. Implement incoming message handling logic.
    - Extend the existing Telegram update handling service.
    - For text messages:
        - extract the candidate URL
        - validate it
    - If the URL is invalid:
        - send a user-friendly Telegram message asking for a valid regular YouTube video link
    - If the update type is unsupported:
        - ignore it safely

3. Add or extend Telegram API client/service.
    - Implement sendMessage method for plain text bot replies
    - Keep Telegram API integration encapsulated in one dedicated service

4. Keep architecture clean.
    - Controller remains thin
    - Validation is in a separate service
    - Telegram reply logic is not duplicated
    - No yt-dlp execution yet
    - No queue dispatch yet unless a placeholder already exists

5. Add automated tests.
    - Cover valid accepted URLs
    - Cover invalid URLs
    - Cover Shorts rejection
    - Cover playlist rejection
    - Cover user-facing response behavior for invalid messages where practical

6. Update README if needed.
    - Document which YouTube URL formats are accepted
    - Mention explicitly that Shorts and playlists are not supported in this version

Important constraints:
- Do not add database
- Do not implement subtitle downloading yet
- Do not implement Redis locking yet
- Do not implement queue worker logic yet
- Keep the code ready for later async job dispatch

After implementing:
- Run relevant tests inside Docker
- Ensure code style matches the project conventions

At the end, generate a commit message in English describing the changes.
