#!/usr/bin/env php
<?php

require dirname(__DIR__).'/vendor/autoload.php';

if (!is_file(dirname(__DIR__).'/vendor/bin/phpunit')) {
    fwrite(STDERR, "Unable to find vendor/bin/phpunit. Did you run composer install?\n");
    exit(1);
}

require dirname(__DIR__).'/vendor/bin/phpunit';

